namespace Innovayse.Application.Migration.Queries.TestMigrationConnection;

/// <summary>Tests the connection from the panel to the migration plugin at SourceUrl.</summary>
public sealed record TestMigrationConnectionQuery(int Id);
