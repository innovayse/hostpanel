# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Local tooling & planning
.claude/
.planning/

# npm is not this app's package manager. Its lockfile is read by neither CI nor the
# Docker build, so it only ever drifts and then breaks the first person who trusts it.
package-lock.json
